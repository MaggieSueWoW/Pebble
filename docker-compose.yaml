services:
  pebble-loop:
    container_name: pebble-loop
    image: pebble-loop:latest
    build:
      context: .
      dockerfile: Dockerfile
    command: ["loop", "--config", "/app/config-TWW-S3.yaml", "--trigger-timeout", "5"]
    restart: unless-stopped
    env_file:
      - .env
